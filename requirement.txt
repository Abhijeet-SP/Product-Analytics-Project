# Python dependencies for script/db_generator.py (synthetic data generation only).
#
# Everything else in this project is SQL (PostgreSQL 14+, run with psql) or
# Tableau Desktop, neither of which needs Python.
#
# Install:  python3 -m pip install -r requirement.txt
# Verified: Python 3.13 on macOS (arm64), pinned to the versions used to
#           generate the CSV snapshot in 01_data_csv/.

pandas==2.2.3
numpy==2.1.3
Faker==40.28.1
tqdm==4.68.3
